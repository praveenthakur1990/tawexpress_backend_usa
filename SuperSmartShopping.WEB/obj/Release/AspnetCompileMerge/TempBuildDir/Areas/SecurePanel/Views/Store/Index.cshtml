@model IEnumerable<SuperSmartShopping.DAL.ViewModels.TenantModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/SecurePanel/Views/Shared/_LayoutSecurePanel.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="row pageTitle">
            <div class="col-lg-6">
                <h1>Store List</h1>
            </div>
            <div class="col-lg-6 text-right">
                <button class="btn btn-primary" onclick="window.location.href='@Url.Action("AddUpdate","Store",new {area="securepanel" })'"><i class="sm-add"></i>Add New</button>
            </div>
        </div>
        <div class="ibox-content">
            <div class="table-responsive">
                <table class="table table-hover dataTables-example">
                    <thead>
                        <tr>
                            <th>Email</th>
                            <th>UserId</th>
                            <th>Added By (RoleName)</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Count() > 0)
                        {
                            foreach (var item in Model)
                            {
                                <tr class="gradeA">
                                    <td>@item.TenantName</td>
                                    <td>@item.UserId</td>
                                    <td>@item.AddedByName (@item.RoleName)</td>
                                    <td class="center">
                                        <a href="@Url.Action("AddUpdate","Store",new {area="SecurePanel",userId=item.UserId, subDomain=item.TenantDomain })" class="editButon"><i class="fa fa-edit"></i>Edit</a>
                                        <a href="@Url.Action("AdvertisementImage","Store",new {area="SecurePanel",userId=item.UserId })" class="editButon"><i class="fa fa-edit"></i>Adv Image</a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="4">
                                    <div class="alert alert-danger text-center" role="alert">
                                        No data found
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>